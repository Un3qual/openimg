
<% if flash.any? %>
  <div class="flashes">
    <% user_facing_flashes.each do |key, value| -%>
      <div class="notification is-primary flash-<%= key %>"><%= value %></div>
    <% end -%>
  </div>
<% end %>
