<p id="notice"><%= notice %></p>

<h1>Flags</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Comment</th>
      <th>Reason</th>
      <th>Image</th>
      <th>Action taken</th>
      <th>Action</th>
      <th>Action taken by</th>
      <th>Action comment</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @flags.each do |flag| %>
      <tr>
        <td><%= flag.user %></td>
        <td><%= flag.comment %></td>
        <td><%= flag.reason %></td>
        <td><%= flag.image %></td>
        <td><%= flag.action_taken %></td>
        <td><%= flag.action %></td>
        <td><%= flag.action_taken_by %></td>
        <td><%= flag.action_comment %></td>
        <td><%= link_to 'Show', flag %></td>
        <td><%= link_to 'Edit', edit_flag_path(flag) %></td>
        <td><%= link_to 'Destroy', flag, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Flag', new_flag_path %>
